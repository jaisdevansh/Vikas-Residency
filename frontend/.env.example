# Express backend URL accessed from the Next.js server (Node.js environment)
BACKEND_URL=http://127.0.0.1:5000

# Express backend URL accessed directly from the user's browser (client-side)
NEXT_PUBLIC_API_URL=http://localhost:5000
